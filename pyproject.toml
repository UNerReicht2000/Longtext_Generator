[project]
name = "langtext-generator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyqt6>=6.7.1",
]

[dependency-groups]
dev = [
    "pytest-cov>=7.0.0",
    "pytest>=8.3.3",
    "pytest-qt>=4.5.0",
    "pyinstaller>=6.16.0",
]
